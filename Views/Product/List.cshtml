@model IEnumerable<Product>

@{
}

@foreach(Product p in Model){
    <div>
        <h3>@p.Name</h3>
        <p>@p.Description</p>
        <h4>@p.Price.ToString("c")</h4>
    </div>
    <br>
}